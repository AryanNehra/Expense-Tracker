# Example environment variables for Expense Tracker App

# MongoDB connection string (replace with your actual URI)
MONGO_URI=your_mongodb_connection_here

# JWT secret for authentication tokens
JWT_SECRET=your_jwt_secret_here

# Port for running the backend server
PORT=5000

# 256-bit encryption key (32 bytes = 64 hex characters)
# You can generate it using: openssl rand -hex 32
ENC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef(this is just an example)
